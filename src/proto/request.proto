syntax = "proto3";

package CodecServer.proto;

message Request {
  string codec = 1;
  enum Direction {
    ENCODE = 0;
    DECODE = 1;
  }
  repeated Direction direction = 2;
  map<string, string> args = 3;
}